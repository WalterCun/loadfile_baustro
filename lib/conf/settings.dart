const bool DEBUG = false;

const Map DATABASES = {
  'default': {
    'NAME': 'floadfile.db',
    'USER': null,
    'PASSWORD': null,
    'HOST': null,
    'PORT': null,
  }
};

const String VERSION = '0.0.1';
late final iVERSION = int.tryParse(VERSION);

// DEFAULT_FROM_EMAIL = 'waltercunbustamante@gmail.com'
